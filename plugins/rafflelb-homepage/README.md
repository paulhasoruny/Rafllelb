# RaffleLB Homepage 0.1.2

Optimizes the existing `hero-reference-scene.webp` for web delivery while preserving the same filename, 1536×1024 dimensions, and homepage behavior. The hero is recompressed as high-quality WebP (quality 92) for a major size reduction with no obvious visual degradation in normal website use.

Upload the ZIP and replace the existing RaffleLB Homepage plugin in its current folder, then clear site/page caches. Keep Core, Account, Shop and Draw Engine active.

No migrations, database changes, hook changes, shortcode changes, or URL changes are included in this release.

Rollback: replace Homepage with the previous 0.1.1 ZIP.
